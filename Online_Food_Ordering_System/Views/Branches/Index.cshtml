@model IEnumerable<Online_Food_Ordering_System.Models.Entity.Branch>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.brnName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.brnAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.brnName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.brnAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BID })
        </td>
    </tr>
}

</table>
